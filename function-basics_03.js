// Write a function min(a,b) which returns the least of two numbers a and b.

// if-else
function getMin(a, b) {
  if (a < b) {
    return a;
  } else {
    return b;
  }
}

// question mark operator
function getMin(a, b) {
  return (a < b) ? a 
  : b;
}
