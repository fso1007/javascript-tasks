// Create a script that prompts the visitor to enter two numbers and then shows their sum.

let number_a = +prompt("Type a number: ");
let number_b = +prompt("Type another one: ");

alert(number_a + number_b);